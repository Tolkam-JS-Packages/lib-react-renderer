// component name to render
export const ATTR_COMPONENT = 'data-rr';

// root element html tag name
export const ATTR_TAG = 'data-rr-tag';

// class name of created root element
export const ATTR_CLASS = 'data-rr-class';

// container group id root element belongs to
export const ATTR_CONTAINER_GROUP = 'data-rr-cid';

// JSON props attribute
export const ATTR_PROPS = 'data-props';

// single prop attribute prefix
export const ATTR_PROP = 'data-prop-';

// attribute prefix to get from global object (window)
export const ATTR_PROP_REF = 'data-g-prop-';

// name of the prop for removed placeholder on root element
export const PH_PROP = 'rrPlaceholder';
